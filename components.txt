|- App [ membersState ]
    |- Members info ( { member, setMembersState } )
        |- Member info ( { member, setMembersState } )
            |- Edit member info ( { member, setMembersState } )
             \ Transaction List ( { member, setMembersState } )
                |- SyncTable ( {member, transactions, setMembersState} )
                    |- getColumns( {transactions, member, setMemberState} )
                |- New Transaction ( { member, setMembersState } )
        |- MemberNew ( { membersState } )

    |- Repayments
        |- Repayments List

    |- Reset
